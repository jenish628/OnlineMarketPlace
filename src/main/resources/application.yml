spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pmdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Root@1234}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          time_zone: UTC
  jackson:
    serialization:
      fail-on-empty-beans: false
  mail:
    host: smtp.gmail.com
    username: ${EMAIL_USERNAME:sanjayakoju42@gmail.com}
    password: ${EMAIL_PASSWORD:fxahxqzfghwliack}
    properties:
      mail:
        smtp:
          auth: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false

app:

  domainUrl: http://localhost:4200

  cors:
    allowed-origins:
      - http://localhost:4200
      - http://localhost:8080
  jwt:
    secret-key: ${JWT_SECRET_KEY:pm490-proj-secret}
    expire-seconds: 86400

minio:
  bucket:
    name: pmstorage
  access:
    key: ${MINIO_KEY:QZP9OT5EbEV2zqHv}
    secret: ${MINIO_SECRET:fzFsh2kebmBxFDJR0HYdCZERUaF2cq8j}
  url: http://127.0.0.1:9000

